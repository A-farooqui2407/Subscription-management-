# Optional: full API base (production or when not using Vite proxy).
# Leave unset in local dev to use same-origin /api → Vite proxy → backend (see vite.config.js).
# VITE_API_URL=http://localhost:3000/api
# Backend port when proxying (default 3000):
# VITE_PROXY_TARGET=http://localhost:3000
